'use strict';

function swam(items, firstIndex, secondIndex) {
  var temp = items[firstIndex];
  var items[firstIndex] = items[secondIndex];
  var items[secondIndex] = temp;
}

function partition(items, left right) {

}

function quickSort(items, left, right) {
  
}
